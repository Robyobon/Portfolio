

const ContactButton = () => {
  const emailAddress = 'Robyhdyt97@gmail.com'; // Ganti dengan alamat email Anda

  return (
    <a href={`mailto:${emailAddress}`} id="contack">
    </a>
  );
};

export default ContactButton;
